import type { ActivityFilter, CustomerActivity } from '../types/customerActivity'
export const filterCustomerActivities = (items: CustomerActivity[], filter: ActivityFilter): CustomerActivity[] => { const search = filter.search?.trim().toLowerCase(); const from = filter.from ? Date.parse(filter.from) : -Infinity; const to = filter.to ? Date.parse(filter.to) : Infinity; return items.filter((item) => { const metadata = Object.entries(item.metadata ?? {}).map(([key, value]) => `${key} ${String(value)}`).join(' '); const searchable = [item.description, item.actor.name, item.actor.department, item.module, item.action, item.relatedRecord?.referenceNumber, item.reason, metadata].filter(Boolean).join(' ').toLowerCase(); const actorMatch = !filter.actor || (filter.actor === 'Marketing Employees' ? item.actor.type === 'SEDAR Employee' && item.actor.department === 'Marketing' : filter.actor === 'Other SEDAR Departments' ? (item.actor.type === 'SEDAR Employee' && item.actor.department !== 'Marketing') || item.actor.type === 'SEDAR Department' : filter.actor === 'Customer-Originated' ? item.actor.type === 'Customer' : item.actor.type === 'System'); const time = Date.parse(item.occurredAt); return (!filter.module || item.module === filter.module) && (!filter.action || item.action === filter.action) && actorMatch && (!filter.visibility || item.visibility === filter.visibility) && time >= from && time <= to && (!search || searchable.includes(search)) }) }
