import type { CustomerActivity } from '../types/customerActivity'
export const getCustomerActivityRoute = (item: CustomerActivity): string | undefined => { const id = encodeURIComponent(item.customerId); const recordId = item.relatedRecord?.id ? encodeURIComponent(item.relatedRecord.id) : undefined; if (item.module === 'Customer Account') return `/marketing/customers/${id}`; if (item.module === 'Contacts') return `/marketing/customers/${id}?tab=contacts`; if (item.module === 'Service Requests' && recordId) return `/marketing/service-requests/${recordId}`; if (item.module === 'Quotations' && recordId) return `/marketing/quotations/${recordId}`; if (item.module === 'Contract Requests' && recordId) return `/marketing/contract-requests/${recordId}`; if (item.module === 'Contracts' && recordId) return `/marketing/contracts/${recordId}`; if (item.module === 'Appointments') return `/marketing/customers/${id}?tab=appointments`; if (item.module === 'Documents') return `/marketing/customers/${id}?tab=documents`; return undefined }
